function reverseString(str) {
    //Split the string with str.split('');
    //Reverse the array with .reverse();
    //Join the reversed array with .join('');
    return str.split('').reverse().join('');
  }
  
  reverseString("hello");